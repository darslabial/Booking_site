services:
  - type: web
    name: booking-site
    env: python
    runtime: python-3.12
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
